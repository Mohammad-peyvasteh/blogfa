export const metadata = {
  title: "ورود",
   
  description: "بلاگفا",
};

export default function RootLayout({ children }) {
  return (
    
      < div>
          
          {children}
        
      </div>
    
  );
}