export const metadata = {
  title: "مدیریت",
   
  description: "بلاگفا",
};

export default function RootLayout({ children }) {
  return (
    
      < div>
          
          {children}
        
      </div>
 
  );
}